{
    "extends": "nx/presets/npm.json",
    "parallel": 3,
    "tasksRunnerOptions": {
        "default": {
            "runner": "nx/tasks-runners/default",
            "options": {
                "cacheableOperations": ["build", "test"]
            }
        }
    },
    "namedInputs": {
        "core_server": [ "{projectRoot}/apps/server/**/*.{ts,json}"],
        "libs": [ "{projectRoot}/lib/**/*.{ts,json}" ],
        "apps": [ "{projectRoot}/apps/!(server)/!(dist|artifacts)/**/*.{ts,json}" ],
        "games": [ "{projectRoot}/games/*/*/!(dist)/**/*.{ts,json}"]
    },
    "targetDefaults": {
        "build": {
            "dependsOn": ["^build"],
            "inputs": ["libs", "apps", "games"]
        },
        "artifacts:copy:package": {
            "dependsOn": ["build"],
            "inputs": ["apps", "games"]
        },
        "watch": {
            "dependsOn": ["bulid"],
            "inputs": ["^core_server", "libs", "apps", "games"]
        }
    },
    "targets": {

        "test": {
            "dependsOn": ["build"]
        }
    }
}
