import { token } from 'ditox';
export const SMG_GAME_SERVER_ROUTERS_TOKEN = token(Symbol.for('Multifactory for Game Server routers'));
export const SMG_GAME_SERVER_ROUTER_FACTORY_TOKEN = token(Symbol.for('Collective value token for game server router factories'));
export const SMG_GAME_SERVER_ARTIFACT_ROOT_TOKEN = token(Symbol.for('Game server artifact root for file serving'));
export const SMG_SERVER_APP_TOKEN = token(Symbol.for('Root Application binding'));
//# sourceMappingURL=index.js.map